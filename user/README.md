# user Directory

This directory contains user-space control programs for loading eBPF code and handling traced output.

Responsibilities:
- Load and attach eBPF programs.
- Receive and aggregate latency data from the kernel side.
- Print tracing results and optionally persist them to files.
